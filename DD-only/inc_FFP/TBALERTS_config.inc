#MP ;Configuration of component TBALERTS
#MP If Defined(optionset {MAC}) 
#MP Else
#MP Macro optionset
#MP Endm
#MP Endif
#MP If Defined(select {MAC}) 
#MP Else
#MP Macro select
#MP Setstr #1# {#2#}
#MP Endm
#MP Endif
#MP optionset[TBALERTS_OPTIONS_CACHE, INCLUDE, EXCLUDE]
#MP optionset[TBALERTS_OPTIONS_DEVIATION_CUTOFF, IGNORED, SUSPENDED, CLEARED ]
#MP optionset[TBALERTS_OPTIONS_HISTORY, INCLUDE, EXCLUDE]
#MP optionset[TBALERTS_OPTIONS_NEARCLOSED_CUTOFF, IGNORED, SUSPENDED, CLEARED ]
#MP select[TBALERTS_SELECTION_CACHE, INCLUDE ]
#MP select[TBALERTS_SELECTION_DEVIATION_CUTOFF, SUSPENDED ]
#MP select[TBALERTS_SELECTION_HISTORY, INCLUDE ]
#MP select[TBALERTS_SELECTION_NEARCLOSED_CUTOFF, SUSPENDED ]
